app:
  build:
    image: flomotlik/discourse
    dockerfile_path: Dockerfile
  environment:
  links:
    - postgresql
    - redis
  cached: true
  volumes:
    - ./:/code
postgresql:
  image: postgres:9.3.6
